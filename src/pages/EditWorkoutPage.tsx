const EditWorkoutPage = () => {
  return (
    <main className="max-w-3xl mx-auto px-4 py-8">
      {/* 
        Temporary route page for editing a workout.
        We are creating this now to fix the App.tsx import error.

        Next, we will replace this placeholder with:
        - URL parameter reading
        - existing workout loading
        - editable form fields
        - API update request
      */}
      <h1 className="text-2xl font-bold text-gray-900 mb-4">
        Edit Workout
      </h1>

      <p className="text-gray-600">
        Edit workout functionality is coming next.
      </p>
    </main>
  );
};

export default EditWorkoutPage;